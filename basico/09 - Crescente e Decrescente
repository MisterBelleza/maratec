while True:
    try:
        tupla = [int(x) for x in input().split()]
        if tupla[0] < tupla[1]: 
            print("Crescente") 
        elif tupla[0] > tupla[1]: 
            print("Decrescente")
        else:
            break
    except:
        pass
